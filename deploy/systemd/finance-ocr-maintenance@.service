[Unit]
Description=Finance OCR maintenance command (%i)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User=finance-ocr
WorkingDirectory=/opt/finance-ocr
Environment=MEELO_PROJECT_DIR=/opt/finance-ocr
ExecStart=/opt/finance-ocr/deploy/maintenance/run_command.sh %i
